/*Algorithm:
Take the given polynomial as an input
Put a random value in the function and check if its positive or negative (put x=0)
if the value turns out to be negative at x=0 then put x=1 */

#include <stdio.h>

void polynomial(int *arr);
void biject(int x, int *arr);
int solution(int x , int *arr);

int main()
{

}

void polynomial(int *arr)
{
    //function to take a polynomial as an input and store it to the provided array

}
void biject(int x, int *arr)
{
    //function to calculate the bijection of the given x
}
