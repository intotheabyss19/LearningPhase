local number = 5
local string = "not a comment, a string instead"
local single = 'also works'

local tooMuchInfo = [[ This is a
multi line and literal ]]

local truth, lies = true, false -- assigning multiple values at same time

local nothing = nil

