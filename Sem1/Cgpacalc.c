#include <stdio.h>
int main()
{
    while (1)
    {
        printf("Enter your choice: ");
        scanf("%d", &ch);
        switch(ch)
        {
            case 1:theory
        }
    }
}